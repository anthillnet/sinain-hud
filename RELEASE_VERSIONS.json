{
  "_comment": "Per-component release versions \\u2014 the single source of truth for the unified release workflow (.github/workflows/release.yml). Each component keeps its own series. Bump the components you want to ship, commit, then push any v* tag (named after the product/DMG version, e.g. v0.3.0). Components whose release tag already exists are skipped, so unchanged components don't re-release.",
  "dmg": "0.4.0",
  "overlay": "2.14.0",
  "npm": "1.28.0",
  "sck-capture": "1.2.2"
}
